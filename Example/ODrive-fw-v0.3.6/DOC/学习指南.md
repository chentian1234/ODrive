# ODrive FOC工程系统学习指南

我将为你制定一个**由浅入深、循序渐进**的学习路径，帮助你快速掌握这个工程。

## 一、学习路线图（建议顺序）

```
第1阶段：理解整体架构（1天）
    ↓
第2阶段：掌握数据结构（1-2天）
    ↓
第3阶段：跟踪代码执行流程（2-3天）
    ↓
第4阶段：深入FOC算法核心（3-5天）
    ↓
第5阶段：动手实践调参（持续）
```

---

## 二、第1阶段：理解整体架构

### 1.1 先看"地图"——工程文件结构

```
MotorControl/          ← 电机控制核心（重点学习）
├── low_level.c/h      ← ??? FOC算法核心
├── commands.c/h       ← ?? 文本命令解析
├── commands_pro.c/h   ← ?? 二进制命令协议
├── datatypes.h        ← ?? 数据类型定义
├── utils.c/h          ← ?? SVM、数学工具
├── packet.c/h         ← ? 通信数据包
├── buffer.c/h         ← ? 数据序列化
├── terminal.c/h       ← ? 终端命令
└── crc.c/h            ← CRC校验

Src/                   ← STM32外设和应用层
├── main.c             ← ?? 程序入口
├── freertos.c         ← ?? 任务创建
├── adc.c/tim.c等      ← 外设配置
```

### 1.2 理解硬件信号流

```
物理世界                    STM32芯片                      控制算法
┌─────────┐              ┌──────────────┐              ┌──────────┐
│ 无刷电机 │←──PWM波形────│  TIM1/TIM8   │←─────────────│ FOC算法  │
│         │              │  (PWM发生器)  │              │          │
│ 三相线  │──电流信号──→│  ADC2/ADC3   │──采样值──→  │ 电流环PI │
│         │              │  (电流采样)   │              │          │
│ 编码器  │──ABZ脉冲──→│  TIM3/TIM4   │──计数值──→  │ 位置速度 │
└─────────┘              └──────────────┘              └──────────┘
                              ↑                              ↑
                         DRV8301驱动                   FreeRTOS任务
```

### 1.3 理解软件架构

```
┌─────────────────────────────────────────────────────────────┐
│                      main() 主函数                          │
│  HAL_Init → 时钟配置 → 外设初始化 → MX_FREERTOS_Init()     │
└────────────────────────┬────────────────────────────────────┘
                         ↓
┌─────────────────────────────────────────────────────────────┐
│              MX_FREERTOS_Init() 创建任务                    │
└────────────────────────┬────────────────────────────────────┘
                         ↓
┌──────────┬──────────┬──────────┬──────────┬────────────────┐
│Motor任务0│Motor任务1│命令解析  │USB处理   │数据包定时器    │
│(最高优先)│(高优先)  │(普通)    │(普通)    │(低优先)        │
└────┬─────┴────┬─────┴────┬─────┴────┬─────┴────────────────┘
     │          │          │          │
     ↓          ↓          ↓          ↓
  FOC控制    FOC控制   串口命令    USB中断
  M0电机     M1电机    解析处理    处理
```

---

## 三、第2阶段：掌握核心数据结构

### 3.1 最重要的结构体——Motor_t

这是整个系统的**核心数据结构**，包含了电机的所有状态和控制参数。

```c
// 在 low_level.h 中定义，low_level.c 中初始化
Motor_t motors[] = {
    {  // M0电机配置
        .control_mode = CTRL_MODE_VELOCITY_CONTROL,  // 控制模式
        .pole_pairs = 7,                             // 极对数（必须正确！）
        .phase_resistance = 0.0f,                    // 相电阻（校准后填入）
        .phase_inductance = 0.0f,                    // 相电感（校准后填入）
        
        // 控制参数
        .pos_gain = 20.0f,                           // 位置环P增益
        .vel_gain = 15.0f / 1000.0f,                 // 速度环P增益
        .vel_integrator_gain = 1.0f,                 // 速度环I增益
        .current_setpoint = 0.0f,                    // 电流给定
        .vel_setpoint = 0.0f,                        // 速度给定
        .pos_setpoint = 0.0f,                        // 位置给定
        
        // 电流环
        .current_control = {
            .current_lim = 10.0f,                    // 电流限制
            .p_gain = 0.0f,                          // 自动计算
            .i_gain = 0.0f,                          // 自动计算
        },
        
        // 转子检测模式
        .rotor_mode = ROTOR_MODE_SENSORLESS,         // 无感模式
        // .rotor_mode = ROTOR_MODE_ENCODER,         // 编码器模式
        
        // 无感观测器参数
        .sensorless = {
            .pm_flux_linkage = 1.43e-3f,             // 永磁体磁链（关键！）
            .spin_up_current = 10.0f,                // 起转电流
        },
    },
    {  // M1电机配置（类似）
        ...
    }
};
```

**学习建议**：把这个结构体打印出来，每个字段都搞清楚含义。

### 3.2 控制模式枚举

```c
// 从低到高的控制级别（可以"<"比较）
typedef enum {
    CTRL_MODE_VOLTAGE_CONTROL = 0,    // 电压控制（最直接）
    CTRL_MODE_CURRENT_CONTROL = 1,    // 电流控制（常用）
    CTRL_MODE_VELOCITY_CONTROL = 2,   // 速度控制（常用）
    CTRL_MODE_POSITION_CONTROL = 3    // 位置控制（最高级）
} Motor_control_mode_t;
```

### 3.3 错误码枚举

```c
typedef enum {
    ERROR_NO_ERROR = 0,                        // 正常
    ERROR_PHASE_RESISTANCE_TIMING = 1,         // 电阻测量超时
    ERROR_ENCODER_RESPONSE = 7,                // 编码器无响应
    ERROR_ADC_FAILED = 9,                      // ADC失败
    ERROR_DRV_FAULT = 20,                      // DRV8301故障
    ERROR_DC_BUS_BROWNOUT = 23,                // 母线欠压
    ...
} Error_t;
```

---

## 四、第3阶段：跟踪代码执行流程

### 4.1 程序启动流程（必读）

```
main() [main.c]
  │
  ├─ 第1步：HAL_Init()
  │   └─ 初始化Flash、SysTick、NVIC
  │
  ├─ 第2步：SystemClock_Config()
  │   └─ HSE 8MHz → PLL → SYSCLK 168MHz
  │
  ├─ 第3步：外设初始化（13个MX_*_Init）
  │   ├─ GPIO：PWM输出、编码器输入、片选等
  │   ├─ ADC1/2/3：电流采样、母线电压采样
  │   ├─ TIM1/8：高级定时器，产生互补PWM
  │   ├─ TIM3/4：编码器接口模式
  │   ├─ SPI3：DRV8301通信
  │   └─ UART4/USB：调试通信
  │
  ├─ 第4步：OC4_PWM_Override()
  │   └─ 配置TIM1/8的OC4用于触发ADC采样
  │
  └─ 第5步：MX_FREERTOS_Init() → osKernelStart()
      └─ 启动RTOS，任务开始运行
```

### 4.2 电机线程执行流程（??? 最重要）

```
motor_thread() [low_level.c 第1463行]
  │
  ├─ 初始化：分配抗齿槽映射内存
  │
  └─ 无限循环 for(;;)
      │
      ├─ 【阶段1：校准】do_calibration == false
      │   ├─ 使能PWM输出
      │   ├─ motor_calibration() [第865行]
      │   │   ├─ measure_phase_resistance() 测电阻
      │   │   ├─ measure_phase_inductance() 测电感
      │   │   └─ calib_enc_offset() 校准编码器
      │   └─ 关闭PWM，标记校准完成
      │
      ├─ 【阶段2：运行】calibration_ok && enable_control
      │   ├─ 使能PWM
      │   ├─ 无感模式：spin_up_sensorless() 起转
      │   ├─ control_motor_loop() 主控制循环 ???
      │   └─ 退出后关闭PWM
      │
      └─ 【阶段3：待机】零电压输出，延时100ms
```

### 4.3 FOC主控制循环（??? 核心中的核心）

```
control_motor_loop() [low_level.c 第1356行]
  │
  ├─ while(enable_control) {
  │   │
  │   ├─ 1. 等待电流采样完成
  │   │   osSignalWait(M_SIGNAL_PH_CURRENT_MEAS)
  │   │
  │   ├─ 2. 故障检查
  │   │   do_checks() → DRV故障、母线欠压
  │   │
  │   ├─ 3. 更新转子位置
  │   │   loop_updates() → update_rotor()
  │   │   ├─ 编码器模式：读取TIM3 → PLL跟踪
  │   │   └─ 无感模式：磁链观测器 → 估算角度
  │   │
  │   ├─ 4. 位置环计算（如果位置控制模式）
  │   │   pos_err = pos_setpoint - pll_pos
  │   │   vel_des += pos_gain * pos_err
  │   │
  │   ├─ 5. 速度限幅
  │   │   vel_des = clamp(vel_des, -vel_limit, vel_limit)
  │   │
  │   ├─ 6. 速度环计算（如果速度控制模式）
  │   │   vel_err = vel_des - get_pll_vel()
  │   │   Iq += vel_gain * vel_err
  │   │
  │   ├─ 7. 速度积分器（抗饱和）
  │   │   Iq += vel_integrator_current
  │   │
  │   ├─ 8. 电流限幅
  │   │   Iq = clamp(Iq, -current_lim, current_lim)
  │   │
  │   └─ 9. FOC电流控制
  │       FOC_current(motor, 0.0f, Iq) ???
  │   }
```

### 4.4 中断触发流程

```
PWM定时器计数到中心点
       ↓
触发ADC注入转换
       ↓
pwm_trig_adc_cb() 回调函数 [low_level.c 第605行]
       ↓
读取ADC值 → 转换为相电流
       ↓
DC_CAL滤波（零漂校准）
       ↓
osSignalSet(motor_thread) 唤醒电机线程
```

---

## 五、第4阶段：深入FOC算法核心

### 5.1 FOC_current函数详解（???）

这是整个工程最核心的函数，实现了完整的FOC算法。

```c
bool FOC_current(Motor_t *motor, float Id_des, float Iq_des) {
    // ====== 第1步：Clarke变换（ABC三相→αβ两相静止）======
    // Ia + Ib + Ic = 0，所以只需要测量两相
    float Ialpha = -motor->current_meas.phB - motor->current_meas.phC;
    float Ibeta = one_by_sqrt3 * (motor->current_meas.phB - motor->current_meas.phC);
    
    // ====== 第2步：Park变换（αβ静止→dq旋转坐标系）======
    float phase = get_rotor_phase(motor);  // 转子电角度θ
    float c = arm_cos_f32(phase);          // cos(θ)
    float s = arm_sin_f32(phase);          // sin(θ)
    
    // Park变换公式：
    // Id = Iα·cos(θ) + Iβ·sin(θ)
    // Iq = Iβ·cos(θ) - Iα·sin(θ)
    float Id = c * Ialpha + s * Ibeta;
    float Iq = c * Ibeta - s * Ialpha;
    
    // ====== 第3步：电流PI控制 ======
    float Ierr_d = Id_des - Id;  // d轴电流误差
    float Ierr_q = Iq_des - Iq;  // q轴电流误差
    
    // PI控制器：V = Kp·e + Ki·∫e
    float Vd = ictrl->v_current_control_integral_d + Ierr_d * ictrl->p_gain;
    float Vq = ictrl->v_current_control_integral_q + Ierr_q * ictrl->p_gain;
    
    // ====== 第4步：调制饱和处理（抗积分饱和）======
    float mod_scalefactor = 0.80f * sqrt3_by_2 / sqrtf(mod_d*mod_d + mod_q*mod_q);
    
    if (mod_scalefactor < 1.0f) {
        // 过调制：按比例缩小，衰减积分器
        mod_d *= mod_scalefactor;
        mod_q *= mod_scalefactor;
        ictrl->v_current_control_integral_d *= 0.99f;
        ictrl->v_current_control_integral_q *= 0.99f;
    } else {
        // 正常：更新积分器
        ictrl->v_current_control_integral_d += Ierr_d * i_gain * dt;
        ictrl->v_current_control_integral_q += Ierr_q * i_gain * dt;
    }
    
    // ====== 第5步：反Park变换（dq旋转→αβ静止）======
    // 反Park变换公式：
    // Vα = Vd·cos(θ) - Vq·sin(θ)
    // Vβ = Vq·cos(θ) + Vd·sin(θ)
    float mod_alpha = c * mod_d - s * mod_q;
    float mod_beta = c * mod_q + s * mod_d;
    
    // ====== 第6步：SVM调制 → 计算PWM占空比 ======
    queue_modulation_timings(motor, mod_alpha, mod_beta);
    
    return true;
}
```

### 5.2 坐标系变换图解

```
 Clarke变换          Park变换
ABC坐标系  →→→   αβ静止坐标系  →→→   dq旋转坐标系
(三相交流)      (两相交流)         (两相直流)
                                      ↑
                                 好处：
                                 交流变直流
                                 PI控制无静差
```

### 5.3 SVM空间矢量调制

```
目的：将Vα、Vβ转换为三相PWM占空比(tA, tB, tC)

原理：
- 三相逆变器有8种开关状态（23）
- 6个有效矢量 + 2个零矢量
- 通过伏秒平衡原理合成目标电压矢量

扇区判断（6个扇区）：
        V3(010)
          |
    V2(110)---V4(011)
         \ | /
    ------O------  α轴
         / | \
    V1(100)---V5(001)
          |
        V6(101)
```

---

## 六、第5阶段：动手实践

### 6.1 快速上手步骤

```
第1步：修改电机参数
    → 打开 low_level.c 第63行 motors[] 数组
    → 修改 pole_pairs（极对数）
    → 修改 encoder_cpr（编码器分辨率）

第2步：编译烧录
    → Keil MDK打开 project.uvprojx
    → F7编译 → Ctrl+F8下载

第3步：串口调试
    → 连接USB或UART4
    → 发送命令 "v 0 1000.0 0.0" （M0速度1000）
    → 观察电机运行

第4步：参数调优
    → 读取当前参数 "get f 5" （vel_gain）
    → 修改参数 "set f 5 0.02"
    → 观察效果
```

### 6.2 关键参数速查表

| 参数 | 位置 | 作用 | 调大效果 | 调小效果 |
|------|------|------|---------|---------|
| pole_pairs | motors[0] | 极对数 | - | - |
| current_lim | current_control | 电流限制 | 力矩大 | 力矩小 |
| vel_gain | motors[0] | 速度P增益 | 响应快/易振荡 | 响应慢/稳定 |
| vel_integrator_gain | motors[0] | 速度I增益 | 消除静差/易振荡 | 有静差/稳定 |
| pos_gain | motors[0] | 位置P增益 | 定位快/易振荡 | 定位慢/稳定 |
| pm_flux_linkage | sensorless | 永磁体磁链 | - | - |

### 6.3 调试技巧

```bash
# 1. 查看电机状态（使用监控槽位）
m 0 1 0     # 槽位0: M0位置给定
m 0 25 1    # 槽位1: M0编码器角度
m 0 45 2    # 槽位2: M1编码器角度
o 3         # 输出3个监控值

# 2. 查看设备信息
i           # 显示芯片ID、Flash大小、UUID

# 3. 急停
h           # 所有电机停止

# 4. 抗齿槽校准
t           # 开始抗齿槽校准
```

---

## 七、学习代码的建议

### 7.1 阅读顺序（从简单到复杂）

```
1. datatypes.h        ← 看枚举，了解有哪些命令
2. low_level.h        ← 看结构体，了解系统数据
3. main.c             ← 看启动流程
4. freertos.c         ← 看任务创建
5. low_level.c        ← 重点！跟踪motor_thread
6. utils.c            ← 理解SVM算法
```

### 7.2 调试方法

```
方法1：串口打印
    → 在关键位置添加 commands_printf()
    → 观察变量变化

方法2：监控槽位
    → 配置monitoring_slots
    → 实时输出多个变量

方法3：Keil调试器
    → 设置断点
    → 查看变量值
    → 单步执行
```

### 7.3 常见问题排查

| 现象 | 可能原因 | 排查方法 |
|------|---------|---------|
| 编译报错 | 头文件路径 | 检查Include Paths |
| 电机不转 | 未校准 | 检查calibration_ok |
| 电机抖动 | 编码器偏移 | 重新校准或手动调offset |
| 电流过大 | 参数不对 | 检查phase_resistance |
| 无感不转 | 磁链不对 | 检查pm_flux_linkage |

---

## 八、总结

**学习FOC的核心要点**：

1. **理解坐标系变换**：ABC→αβ→dq的物理意义
2. **掌握三环控制**：位置环→速度环→电流环的级联
3. **搞清执行流程**：从main()到FOC_current()的调用链
4. **动手调参**：理论结合实践，观察参数影响

建议你先按照第6.1节的步骤让电机转起来，然后再回头深入理解代码。遇到具体问题随时问我！